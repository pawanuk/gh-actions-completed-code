---
name: Continuous Deployment

on: push

jobs:
  hello-gh-actions:
    name: Hello GitHub Actions
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run Tests
        run: echo "RUNNING TESTS!"
